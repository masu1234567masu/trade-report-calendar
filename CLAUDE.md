# このリポジトリで作業する前に

トレード収支管理アプリ（個人利用・スタンドアロンHTML/JS）。過去に「動くはず」で報告してユーザーの実機で何度も再現した不具合があり、その教訓をここに残す。**画面・操作に関わる変更をする際は、このファイルを踏まえた上で作業すること。**

## 絶対に守ること

**UI/JSに変更を加えたら、pushする前に必ず `test/smoke-test.js` を実行し、実際にヘッドレスブラウザで操作して確認してから報告する。** コードを目で読んで「合っているはず」で報告するのは禁止。過去に何度もそれで見た目には気づけないCSS/ブラウザ固有の不具合を見逃し、ユーザーに何度も同じ実機テストをさせてしまった。

```bash
python3 -m http.server 8123 &
NODE_PATH=/opt/node22/lib/node_modules node test/smoke-test.js
```

(この開発環境ではPlaywrightは`/opt/node22/lib/node_modules`にグローバル導入済み。`npm install playwright`でバージョン違いを取得しないこと。理由は下記デバッグツール節参照。)

新しい画面(グラフ・分析など)を追加したら、`test/smoke-test.js` にもその画面の操作確認を追加すること。

**`index.html`を変更したら、`build-footer`の文字列と、`css/style.css`・`js/*.js`のsrc末尾`?v=N`を必ずセットで同じ番号に更新すること。**(詳細は下記5番)

## 過去に実際に起きた不具合と原因(同じミスを繰り返さないための記録)

### 1. `hidden`属性がCSSの`display`指定に上書きされる
`.modal-overlay { display: flex; }` や `#signed-in-area { display: flex; }` のように、要素にクラスで`display`を直接指定すると、HTML側の`hidden`属性(`display:none`)がCSSカスケードの優先順位で負けて効かなくなる。**author stylesheetの`display`指定は、`[hidden]`というUAスタイルより常に優先される。** 結果、非表示のはずのモーダルが常に画面全体を覆ってクリックを妨害したり、ログイン済み表示とログインボタンが同時に見えたりした。

**対策**：`display`を条件付きで切り替えたい要素には、必ず `セレクタ[hidden] { display: none; }` を明示的に追加すること。JSで`el.hidden = true/false`を使うすべての要素で、CSS側に`display`指定があるかどうかを毎回確認する。

### 2. Google Identity Services (GIS) の`async defer`読み込みとポップアップ認証がモバイルSafariと相性最悪
最初の実装は`google.accounts.oauth2.initTokenClient` + ポップアップでログインする方式だった。これは:
- GISスクリプトを`async defer`で読み込んでいたため、読み込み完了前にログインボタンを押すと`google`が未定義で例外が起き、エラー表示もなく「何も起きない」ように見えた。
- ポップアップ自体がiOS Safariでブロックされやすく、ブロックされてもエラーコールバックが呼ばれず無反応になることがある。

**対策として、認証はポップアップを一切使わない「リダイレクト方式」に変更済み**(`js/auth.js`の`Auth.login()`/`Auth.consumeRedirectResult()`)。ページ全体をGoogleのOAuth画面に遷移させ、`redirect_uri`付きで戻ってきたURLのフラグメントからトークンを受け取る。この方式を維持すること。ポップアップ方式に戻さない。

Google Cloud側では「承認済みのリダイレクトURI」に、末尾スラッシュまで含めて本番URL・ローカルURLを完全一致で登録する必要がある(`docs/GOOGLE_OAUTH_SETUP.md`参照)。

### 3. iOS Safariでボタンがダブルタップ＝ズーム扱いになりタップが効かない
`touch-action`を指定していないと、iOS Safariがボタン等の要素を「ズーム対象の普通のコンテンツ」として扱うことがあり、シングルタップが無反応・ダブルタップで拡大されるだけ、という分かりにくい壊れ方をする。**この症状(ダブルタップでズームする)が出たら、まずtouch-actionの指定漏れを疑うこと。**

**対策**：`button, summary, .cal-cell` に `touch-action: manipulation;` を指定済み(`css/style.css`)。新しくタップ操作を伴う要素を追加したら、同様に指定すること。

### 4. エラーメッセージが折りたたみ(`<details>`)の中のログにしか出ていなかった
接続設定パネルは`<details>`で折りたたまれており、その中の`#log-area`にしかエラーを出していなかったため、実際にはエラーが起きていても画面には何も変わったように見えなかった。

**対策**：ログイン関連など重要なエラーは、常に見える`#global-status`(画面上部)にも表示するようにした(`showGlobalError()`)。今後、新しいエラーメッセージを追加する際も、それが折りたたみの中だけに埋もれないか確認すること。

### 5. ビルド表示の更新忘れでキャッシュ判定を誤らせた
`index.html`下部の`build-footer`は「実機で見ているのが本当に最新版か」を確認するための目印だが、グラフ画面・インポート機能を追加した際にこの文字列を更新し忘れ、ユーザーが最新版を見ているのに古いbuild表示のままになり、キャッシュ問題だと誤解させてしまった。

**対策**：`index.html`を変更する際は、
1. `build-footer`の文字列
2. `css/style.css`と`js/*.js`のscript/link src末尾の`?v=N`

を**必ずセットで**同じ番号に更新すること。`?v=N`のクエリ文字列は、HTMLとJS/CSSが別々にキャッシュされて食い違う(古いHTML+新しいJSのような組み合わせで要素が見つからずエラーになる、等)のを防ぐためのキャッシュバスターとして機能する。

### 6. 設定(localStorage)は端末・ブラウザごとに別
OAuthクライアントID・スプレッドシートIDはlocalStorage保存のため、パソコンで設定してもiPhoneには反映されない(逆も同様)。「設定したのに動かない」と言われたら、まずその端末で接続設定が空になっていないかを疑うこと。

### 7. Chart.js をCDN(cdnjs)から読み込んでいたため、ユーザーの実機ネットワークでも読み込みに失敗した
開発環境からcdnjs.cloudflare.comへ到達できないことは把握していたが、「本番のGitHub Pages上なら普通のインターネット接続で到達できるはず」という前提で本番もCDN読み込みのままにしていた。ところが実際にはユーザーの実機(自宅ネットワーク・広告ブロッカー等)でもcdnjsへの通信がブロックされ、グラフ画面が(エラー表示は出るようになったが)動かない状態になった。CDNは「開発環境で届かない」だけでなく「一部ユーザーの実際の環境でも届かない」ことがあり得ると考えるべきだった。

**対策**：Chart.jsは外部CDNに依存せず、`js/vendor/chart.umd.js`としてリポジトリに同梱し、ローカルパスから読み込むように変更した。これによりネットワーク環境・広告ブロッカー等に左右されなくなる。今後、外部ライブラリを追加する際も、個人利用のスタンドアロンアプリという性質上、基本的には同様にリポジトリへ同梱する方針を優先すること(npmレジストリから`npm pack <pkg>@<version>`で取得し、`dist/`内のUMDビルドを`js/vendor/`に配置する)。

### 8. ログイン成功後、この端末で接続設定(スプレッドシートID)が未入力だとエラーが折りたたみの中のログにしか出ず、画面上は「ログイン済みなのに何も表示されない」ように見えた
`loadCalendarData()`はログイン成功直後に自動で呼ばれるが、スプレッドシートID未設定時のエラーを`log()`(折りたたみ内の`#log-area`)にしか出していなかった。ヘッダーはログイン済み表示に切り替わるが、カレンダー・グラフは「ログインすると表示されます」のままで、ユーザーからは矛盾した壊れた状態に見えた。これは4番と同種の「折りたたみの中にしかエラーが出ない」問題の再発であり、かつ6番の「設定は端末ごとに別」が組み合わさって起きた。

**対策**：`loadCalendarData()`のスプレッドシートID未設定時のエラーは`showGlobalError()`で画面上部に出し、あわせて`#settings-details`を自動で開くようにした。

なお、この直後に**同じ構造の別ケース**も見つかった:スプレッドシートIDは設定済みでも、Sheets API自体が(401以外の)エラー(例: 400 `Unable to parse range`等)を返した場合も`log()`だけで、画面上部には何も出ていなかった。`loadCalendarData()`の`catch`節を、401(認証切れ)以外の**すべての**エラーで`showGlobalError()`を呼ぶよう一般化した。**教訓**：ログイン直後・初期化時など、ユーザーが操作していないのに自動で走る処理でエラーが起きうる箇所は、想定しているエラーの種類(この場合「設定未入力」)だけでなく、その関数が投げうるすべてのエラーを可視化すること。一部のケースだけ直して満足しない。

### 9. 「毎回ログインし直すのが面倒」というフィードバックへの対応(ログイン状態のキャッシュ)

初期実装では、アクセストークンをJS変数にしか持たず(サーバーを持たない静的サイトのため)、ページを開き直すたびに毎回「Googleでログイン」を要求していた。ユーザーから手間だと指摘され、以下の2段構えで解消した(`js/auth.js`, `js/app.js`)。

1. **トークンのキャッシュ**: ログイン成功時にアクセストークンと有効期限を`localStorage`に保存する(`saveAccessToken()`)。ページを開き直しても、有効期限内(60秒の余裕を持たせて判定)なら`loadCachedAccessToken()`で再ログインなしに復元する(`restoreSession()`)。
2. **タップ不要の自動再ログイン**: キャッシュも切れている場合、`Auth.loginSilent()`(OAuthの`prompt=none`)で同意画面を出さずに裏でトークンだけ取り直そうとする。ブラウザにGoogleのログインセッションが残っていれば、ユーザーは何もタップせずに戻ってくる。失敗しても(`consumeRedirectResult()`の`silent`フラグで判定)、エラー表示はせず通常の「Googleでログイン」ボタンを見せるだけにする(自動試行の失敗を毎回赤字で見せるのはノイズになるため)。

**注意点**：「ログアウト」ボタンを押したときは`localStorage`に`trc_explicit_logout`フラグを立てる。これが無いと、ログアウトしても次に開いた瞬間に自動再ログイン(2番目の仕組み)が効いてしまい、ログアウトボタンが実質意味を失う。このフラグは次に(手動で)ログインが成功した時点でクリアされる。今後この認証まわりを触るときは、「キャッシュを保存/復元する処理」と「明示的ログアウトを尊重する処理」の両方を必ずセットで確認すること。

**トレードオフの自覚**：アクセストークンを`localStorage`に保存するのは、サーバーを持たない個人利用アプリとしての妥協。すでに接続設定(スプレッドシートID等)も同じ`localStorage`に保存しているため、追加のリスクは限定的と判断した。バックエンドを持たない以上、リフレッシュトークンを使う正攻法(Authorization Codeフロー)はクライアントシークレットの秘匿ができず使えない。

### 10. カレンダーの日次損益が改行されて見づらい問題(土日は必ずしも0ではない)

ユーザーから「土日は絶対0だし」という前提で、平日側の列幅を広げてよいと言われたが、実際の494日分のデータを調べると、まれに(494日中5日)土日にも非ゼロの損益が記録されていた(土日に純資産額を記帳した記録があると、そこまでの差分が損益として計算されるため)。「絶対0」という思い込みだけでCSSの列幅比率を決めると、その5日分だけ数字が箱に収まらず見た目が壊れる。

**対策**：
1. `js/calendar.js`に`formatYenCompact()`を追加し、カレンダーのマス目だけ「円」を付けない表記にした(月合計等、余裕がある場所は従来通り`formatYen()`で「円」を付ける)。
2. `.calendar-grid`の列幅は土日をやや狭く・平日をやや広くする程度(`0.85fr`/`1.06fr`)に留めた。
3. それでも稀に収まらない場合に備えて、`shrinkToFit()`(js/calendar.js)で、その日のマスだけ実際にはみ出しているかを`scrollWidth`で判定し、収まるまで自動で文字を縮小する処理を追加した。CSSの比率だけで「稀な外れ値」に対応しようとせず、実データを検証してから決めること。
4. `test/smoke-test.js`に、インポート済み全期間の全月を巡回して`.cal-pl`がはみ出していないか機械的に確認するテストを追加した(`カレンダーの日次損益が全期間ではみ出し・折り返しなく表示された`)。**見た目のレイアウト調整は、目視だけでなく実データ全件を対象にした自動チェックで検証すること**(今回のように稀なケースは目視では見落としやすい)。

### 11. 複数画面で同じCSSクラス名のボタンを使うと、`querySelectorAll`がタブをまたいで衝突する

分析画面をグラフ画面と同じ「期間タブ+前後ナビゲーション」UIに作り直した際、両画面とも`.period-btn`(月間/年間/全期間などのボタン)という同じクラス名を使っている。`GraphView`は`document.querySelectorAll(".period-btn")`のようにタブで絞り込まずに要素を取得していたため、これは元々グラフ画面にしかこのクラスが無かった間は問題なかったが、分析画面にも同じクラスのボタンを追加した瞬間、`GraphView`のクリックイベントが**非表示の分析画面のボタンにも**付いてしまい、分析画面のタブを押すとグラフ側の状態(`periodType`やactiveクラス)まで意図せず変わってしまう不具合になっていた。

**対策**：`GraphView`・`AnalysisView`とも、要素取得を`document.querySelectorAll("#tab-graph .period-btn")` / `document.querySelectorAll("#tab-analysis .period-btn")`のように、必ず自分の画面のセクションIDで絞り込むよう修正した。**今後、複数の画面で見た目をそろえるために同じクラス名を使う場合は、`querySelectorAll`/`querySelector`を画面のセクションIDでスコープすることを必ず確認すること。** テスト(`test/smoke-test.js`)でも、`page.click('.period-btn[...]')`のような画面を跨ぎうるセレクタは使わず、`'#tab-graph .period-btn[...]'`のようにスコープしたセレクタを使うこと。

## 分析画面(js/analysis.js)の指標計算に関する注意

- 「総リターン」は記録開始時点の純資産額を基準にしているが、入金前(残高0)から記帳を始めたデータだと基準が0になり計算不能になる。その場合は総入金額を基準にフォールバックしている。同様の「基準額が0/未定義になりうる」割り算を追加する際は、必ずnull/Infinityへのフォールバックを入れ、`pctColorClass()`のようにnullを損益どちらか一方に誤分類しないヘルパーを使うこと(`n >= 0`はnullが0扱いされ`true`になるため、素の比較は危険)。
- 年別・月別のドリルダウンで「日」まで降りたときは、新しい日別詳細UIを作らず、カレンダー画面と同じ`EntryModal.open(dateStr)`を再利用している。記帳の編集はどの画面から開いても同じモーダルに統一すること。

## デバッグ時に使えるツール

- ヘッドレスChromiumがこの環境に用意されている(`/opt/pw-browsers/chromium-*/chrome-linux/chrome`)。グローバルに`/opt/node22/lib/node_modules/playwright`としてPlaywright本体も用意されている。**リポジトリの`package.json`が指定するバージョンとこの環境のブラウザビルドが対応しているため、`npm install playwright`でバージョン違いのplaywrightを取得しないこと**(実際に一度`^1.48.0`→`^1.62.1`に上書きしてしまい、ブラウザ実行ファイルが見つからずテストが動かなくなった)。`test/smoke-test.js`は`NODE_PATH=/opt/node22/lib/node_modules node test/smoke-test.js`のようにグローバルの方を使って実行すること。
- ただしこの開発環境からは `accounts.google.com` 等Google本体のドメインへの実通信はネットワークポリシーでブロックされている。本物のGoogleログイン画面をまたぐ部分(リダイレクト後にURLフラグメントへトークンが付いて戻ってくる、という前提)は、実際のURLを直接開いてトークン付きで戻ってきた状態を`page.goto("http://localhost:8123/#access_token=...")`のように再現してテストする(`test/smoke-test.js`参照)。実際にGoogle側と疎通する部分そのものは、この環境では自動テストできない、という限界を認識しておくこと。
- Chart.jsは`js/vendor/chart.umd.js`としてリポジトリに同梱済み(上記7番参照)なので、テスト用の特別なCDN代替設定は不要。
